<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    {# <img src="http://cache.media.education.gouv.fr/image/Logo/99/1/logo_reg_nice_652991.png" style=""> #}
    <a class="navbar-brand" href="{{ path('homepage') }}">Passeport numérique</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        {% if (app.user.roles) is defined %}
            <ul class="navbar-nav ml-auto">
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">{{ app.user.firstName }} - {{ app.user.lastName }}</a>
                    <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
                        <a class="dropdown-item" href="{{ path('contact') }}">Nous contacter</a>
                        <a class="dropdown-item" href="{{ path('account_profile') }}">Profil</a>
                        <a class="dropdown-item" href="{{ path('account_logout') }}">Deconnexion</a>
                    </div>
                </li>
            </ul>
        {% else %}
            {% if app.request.attributes.get('_route') == "account_login" %}
                <ul class="navbar-nav ml-auto">
                    <li class="nav-item active">
                        <a class="nav-link" href="{{ path('homepage') }}">Changer l'établissement<span class="sr-only">(current)</span></a>
                    </li>
                </ul>
            {% endif %}
        {% endif %}
    </div>
</nav>