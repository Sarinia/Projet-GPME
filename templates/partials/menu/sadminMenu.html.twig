<!-- menu principal -->
<div id="menu_principal" class="my-3 d-flex flex-wrap justify-content-center">

	<!-- ROLE_SADMIN -->
	{# <a href="{{ path('passport_show_list') }}" class="mx-1 btn btn-{% if (app.request.attributes.get('_route') | slice(0,8) == "passport") %}success{% else %}primary{% endif %}">Passeport</a> #}
	<a href="{{ path('card_show_list') }}" class="m-1 btn btn-{% if (app.request.attributes.get('_route') | slice(0,4) == "card") %}success{% else %}primary{% endif %}">Fiches SP</a>
	<a href="{{ path('student_show_list') }}" class="m-1 btn btn-{% if (app.request.attributes.get('_route') | slice(0,7) == "student") %}success{% else %}primary{% endif %}">Etudiants</a>
	<a href="{{ path('teacher_show_list') }}" class="m-1 btn btn-{% if (app.request.attributes.get('_route') | slice(0,7) == "teacher") %}success{% else %}primary{% endif %}">Enseignants</a>
	<a href="{{ path('classroom_show_list') }}" class="m-1 btn btn-{% if (app.request.attributes.get('_route') | slice(0,9) == "classroom") %}success{% else %}primary{% endif %}">Classes</a>
	<a href="{{ path('referentiel_list') }}" class="m-1 btn btn-{% if (app.request.attributes.get('_route') | slice(0,11) == "referentiel") %}success{% else %}primary{% endif %}">Référentiel</a>
	<a href="{{ path('admin_show_list') }}" class="m-1 btn btn-{% if (app.request.attributes.get('_route') | slice(0,5) == "admin") %}success{% else %}primary{% endif %}">Administrateurs</a>
	<a href="{{ path('establishment_show_list') }}" class="m-1 btn btn-{% if (app.request.attributes.get('_route') | slice(0,13) == "establishment") %}success{% else %}primary{% endif %}">Etablissements</a>
	<a href="{{ path('department_show_list') }}" class="m-1 btn btn-{% if (app.request.attributes.get('_route') | slice(0,10) == "department") %}success{% else %}primary{% endif %}">Départements</a>

</div>

{# ---------------------------------------------------------------------------------------------------------------------------------------------- #}

<!-- menu secondaire -->
<div id="menu_secondaire" class="my-3">

	<!-- ROLE_SADMIN -->
	<!-- account -->
	{% if (app.request.attributes.get('_route') | slice(0,7) == "account") %}
		<a href="{{ path('account_profile') }}" class="btn btn-{% if app.request.attributes.get('_route') == "account_profile" %}success{% else %}primary{% endif %}">Modification du profil</a>
		<a href="{{ path('account_password') }}" class="btn btn-{% if app.request.attributes.get('_route') == "account_password" %}success{% else %}primary{% endif %}">Modification du mot de passe</a>
	{% endif %}

	<!-- passport -->
	{% if (app.request.attributes.get('_route') == "passport_show") %}
		<a href="{{ path('student_show',{'id':student.id}) }}" class="btn btn-sm btn-primary">retour</a>
		<a href="" class="btn btn-sm btn-primary">Imprimer</a>
	{% endif %}

	<!-- cardsp -->
	{% if (app.request.attributes.get('_route') == "card_show") %}
		<a href="{{ path('card_show_list') }}" class="btn btn-sm btn-primary">Retour à la liste</a>
		<a href="{{ path('card_modify', {'id': card.id}) }}" class="btn btn-sm btn-primary">Modifier</a>
		<a href="{{ path('card_duplicate', {'id': card.id}) }}" class="btn btn-sm btn-primary">Dupliquer</a>
		<a href="" class="btn btn-sm btn-primary">Imprimer</a>
	{% endif %}
	{% if (app.request.attributes.get('_route') == "card_new") %}
		<a href="{{ path('card_show_list') }}" class="btn btn-sm btn-primary">Retour</a>
	{% endif %}
	{% if (app.request.attributes.get('_route') == "card_modify") %}
		<a href="{{ path('card_show', {'id': card.id}) }}" class="btn btn-sm btn-primary">Retour</a>
	{% endif %}
	{% if (app.request.attributes.get('_route') == "card_duplicate") %}
		<a href="{{ path('card_show', {'id': card.id}) }}" class="btn btn-sm btn-primary">Retour</a>
	{% endif %}

	<!-- student -->
	{% if (app.request.attributes.get('_route') == "student_show_list") %}
		<a href="{{ path('student_new') }}" class="btn btn-sm btn-primary">Créer un étudiant</a>
	{% endif %}
	{% if (app.request.attributes.get('_route') == "student_show") %}
		<a href="{{ path('student_show_list') }}" class="btn btn-sm btn-primary">Retour à la liste</a>
		<a href="{{ path('student_modify', {'id': student.id}) }}" class="btn btn-sm btn-primary">Modifier l'étudiant</a>
		{% if student.user.exist == false %}
			<a href=" {{ path('student_delete', {'id': student.id}) }}" class="btn btn-sm btn-danger">Supprimer</a>
		{% endif %}
	{% endif %}
	{% if (app.request.attributes.get('_route') == "student_new") %}
		<a href="{{ path('student_show_list') }}" class="btn btn-sm btn-primary">Retour</a>
	{% endif %}
	{% if (app.request.attributes.get('_route') == "student_modify") %}
		<a href="{{ path('student_show',{'id':student.id}) }}" class="btn btn-sm btn-primary">Retour</a>
	{% endif %}

	<!-- teacher -->
	{% if (app.request.attributes.get('_route') == "teacher_show_list") %}
		<a href="{{ path('teacher_new') }}" class="btn btn-sm btn-primary">Créer un enseignant</a>
	{% endif %}
	{% if (app.request.attributes.get('_route') == "teacher_show") %}
		<a href="{{ path('teacher_show_list') }}" class="btn btn-sm btn-primary">Retour à la liste</a>
		<a href="{{ path('teacher_modify', {'id': teacher.id}) }}" class="btn btn-sm btn-primary">Modifier l'enseignant</a>
		{% if teacher.user.exist == false %}
			<a href=" {{ path('teacher_delete', {'id': teacher.id}) }}" class="btn btn-sm btn-danger">Supprimer</a>
		{% endif %}
	{% endif %}
	{% if (app.request.attributes.get('_route') == "teacher_new") %}
		<a href="{{ path('teacher_show_list') }}" class="btn btn-sm btn-primary">Retour</a>
	{% endif %}
	{% if (app.request.attributes.get('_route') == "teacher_modify") %}
		<a href="{{ path('teacher_show',{'id':teacher.id}) }}" class="btn btn-sm btn-primary">Retour</a>
	{% endif %}

	<!-- classroom -->
	{% if (app.request.attributes.get('_route') == "classroom_show_list") %}
		<a href="{{ path('classroom_new') }}" class="btn btn-sm btn-primary">Créer une classe</a>
	{% endif %}
	{% if (app.request.attributes.get('_route') == "classroom_show") %}
		<a href="{{ path('classroom_show_list') }}" class="btn btn-sm btn-primary">Retour à la liste</a>
		<a href="{{ path('classroom_modify', {'id': classroom.id}) }}" class="btn btn-sm btn-primary">Modifier la classe</a>
		{% if classroom.exist == false %}
			<a href=" {{ path('classroom_delete', {'id': classroom.id}) }}" class="btn btn-sm btn-danger">Supprimer</a>
		{% endif %}
	{% endif %}
	{% if (app.request.attributes.get('_route') == "classroom_new") %}
		<a href="{{ path('classroom_show_list') }}" class="btn btn-sm btn-primary">Retour</a>
	{% endif %}
	{% if (app.request.attributes.get('_route') == "classroom_modify") %}
		<a href="{{ path('classroom_show',{'id':classroom.id}) }}" class="btn btn-sm btn-primary">Retour</a>
	{% endif %}

	<!-- referentiel -->
	{% if (app.request.attributes.get('_route') == "referentiel_list") %}
		<a href="{{ path('referentiel_activity_list') }}" class="m-1 btn btn-sm btn-primary">Activités</a>
		<a href="{{ path('referentiel_problem_list') }}" class="m-1 btn btn-sm btn-primary">Problèmatiques</a>
		<a href="{{ path('referentiel_modality_list') }}" class="m-1 btn btn-sm btn-primary">Modalités</a>
		<a href="{{ path('referentiel_term_list') }}" class="m-1 btn btn-sm btn-primary">Conditions</a>
	{% endif %}
	{% if (app.request.attributes.get('_route') == "referentiel_activity_list") %}
		<a href="{{ path('referentiel_list') }}" class="btn btn-sm btn-primary">Retour</a>
		<a href="{{ path('referentiel_activity_new') }}" class="m-1 btn btn-sm btn-primary">Nouvelle activité</a>
		<a href="{{ path('referentiel_task_new') }}" class="m-1 btn btn-sm btn-primary">Nouvelle tâche</a>
	{% endif %}
	{% if (app.request.attributes.get('_route') == "referentiel_problem_list") %}
		<a href="{{ path('referentiel_list') }}" class="btn btn-sm btn-primary">Retour</a>
		<a href="{{ path('referentiel_problem_new') }}" class="m-1 btn btn-sm btn-primary">Nouvelle problèmatique</a>
	{% endif %}
	{% if (app.request.attributes.get('_route') == "referentiel_modality_list") %}
		<a href="{{ path('referentiel_list') }}" class="btn btn-sm btn-primary">Retour</a>
		<a href="{{ path('referentiel_modality_new') }}" class="m-1 btn btn-sm btn-primary">Nouvelle modalité</a>
	{% endif %}
	{% if (app.request.attributes.get('_route') == "referentiel_term_list") %}
		<a href="{{ path('referentiel_list') }}" class="btn btn-sm btn-primary">Retour</a>
		<a href="{{ path('referentiel_term_new') }}" class="m-1 btn btn-sm btn-primary">Nouvelle condition</a>
	{% endif %}
	{% if (app.request.attributes.get('_route') == "referentiel_activity_new") %}
		<a href="{{ path('referentiel_activity_list') }}" class="btn btn-sm btn-primary">Retour</a>
	{% endif %}
	{% if (app.request.attributes.get('_route') == "referentiel_task_new") %}
		<a href="{{ path('referentiel_activity_list') }}" class="btn btn-sm btn-primary">Retour</a>
	{% endif %}
	{% if (app.request.attributes.get('_route') == "referentiel_problem_new") %}
		<a href="{{ path('referentiel_problem_list') }}" class="btn btn-sm btn-primary">Retour</a>
	{% endif %}
	{% if (app.request.attributes.get('_route') == "referentiel_modality_new") %}
		<a href="{{ path('referentiel_modality_list') }}" class="btn btn-sm btn-primary">Retour</a>
	{% endif %}
	{% if (app.request.attributes.get('_route') == "referentiel_term_new") %}
		<a href="{{ path('referentiel_term_list') }}" class="btn btn-sm btn-primary">Retour</a>
	{% endif %}
	{% if (app.request.attributes.get('_route') == "referentiel_activity_modify") %}
		<a href="{{ path('referentiel_activity_list') }}" class="btn btn-sm btn-primary">Retour</a>
		<a href=" {{ path('referentiel_activity_delete', {'id': activity.id}) }}" class="btn btn-sm btn-danger">Supprimer</a>
	{% endif %}
	{% if (app.request.attributes.get('_route') == "referentiel_task_modify") %}
		<a href="{{ path('referentiel_activity_list') }}" class="btn btn-sm btn-primary">Retour</a>
		<a href=" {{ path('referentiel_task_delete', {'id': task.id}) }}" class="btn btn-sm btn-danger">Supprimer</a>
	{% endif %}
	{% if (app.request.attributes.get('_route') == "referentiel_problem_modify") %}
		<a href="{{ path('referentiel_problem_list') }}" class="btn btn-sm btn-primary">Retour</a>
		<a href=" {{ path('referentiel_problem_delete', {'id': problem.id}) }}" class="btn btn-sm btn-danger">Supprimer</a>
	{% endif %}
	{% if (app.request.attributes.get('_route') == "referentiel_modality_modify") %}
		<a href="{{ path('referentiel_modality_list') }}" class="btn btn-sm btn-primary">Retour</a>
		<a href=" {{ path('referentiel_modality_delete', {'id': modality.id}) }}" class="btn btn-sm btn-danger">Supprimer</a>
	{% endif %}
	{% if (app.request.attributes.get('_route') == "referentiel_term_modify") %}
		<a href="{{ path('referentiel_term_list') }}" class="btn btn-sm btn-primary">Retour</a>
		<a href=" {{ path('referentiel_term_delete', {'id': term.id}) }}" class="btn btn-sm btn-danger">Supprimer</a>
	{% endif %}

	<!-- admin -->
	{% if (app.request.attributes.get('_route') == "admin_show_list") %}
		<a href="{{ path('admin_new') }}" class="btn btn-sm btn-primary">Créer un administrateur</a>
	{% endif %}
	{% if (app.request.attributes.get('_route') == "admin_show") %}
		<a href="{{ path('admin_show_list') }}" class="btn btn-sm btn-primary">Retour à la liste</a>
		<a href="{{ path('admin_modify', {'id': admin.id}) }}" class="btn btn-sm btn-primary">Modifier l'administrateur</a>
		{% if admin.user.exist == false %}
			<a href=" {{ path('admin_delete', {'id': admin.id}) }}" class="btn btn-sm btn-danger">Supprimer</a>
		{% endif %}
	{% endif %}
	{% if (app.request.attributes.get('_route') == "admin_new") %}
		<a href="{{ path('admin_show_list') }}" class="btn btn-sm btn-primary">Retour</a>
	{% endif %}
	{% if (app.request.attributes.get('_route') == "admin_modify") %}
		<a href="{{ path('admin_show',{'id':admin.id}) }}" class="btn btn-sm btn-primary">Retour</a>
	{% endif %}

	<!-- establishment -->
	{% if (app.request.attributes.get('_route') == "establishment_show_list") %}
		<a href="{{ path('establishment_new') }}" class="btn btn-sm btn-primary">Créer un établissement</a>
	{% endif %}
	{% if (app.request.attributes.get('_route') == "establishment_show") %}
		<a href="{{ path('establishment_show_list') }}" class="btn btn-sm btn-primary">Retour à la liste</a>
		<a href="{{ path('establishment_modify', {'id': establishment.id}) }}" class="btn btn-sm btn-primary">Modifier l'établissement</a>
		{% if establishment.exist == false %}
			<a href=" {{ path('establishment_delete', {'id': establishment.id}) }}" class="btn btn-sm btn-danger">Supprimer</a>
		{% endif %}
	{% endif %}
	{% if (app.request.attributes.get('_route') == "establishment_new") %}
		<a href="{{ path('establishment_show_list') }}" class="btn btn-sm btn-primary">Retour</a>
	{% endif %}
	{% if (app.request.attributes.get('_route') == "establishment_modify") %}
		<a href="{{ path('establishment_show',{'id':establishment.id}) }}" class="btn btn-sm btn-primary">Retour</a>
	{% endif %}

	<!-- department -->
	{% if (app.request.attributes.get('_route') == "department_show_list") %}
		<a href="{{ path('department_new') }}" class="btn btn-sm btn-primary">Créer un département</a>
	{% endif %}
	{% if (app.request.attributes.get('_route') == "department_show") %}
		<a href="{{ path('department_show_list') }}" class="btn btn-sm btn-primary">Retour</a>
		<a href="{{ path('department_modify', {'id': department.id}) }}" class="btn btn-sm btn-primary">Modifier le département</a>
		{% if department.exist == false %}
			<a href=" {{ path('department_delete', {'id': department.id}) }}" class="btn btn-sm btn-danger">Supprimer</a>
		{% endif %}
	{% endif %}
	{% if (app.request.attributes.get('_route') == "department_new") %}
		<a href="{{ path('department_show_list') }}" class="btn btn-sm btn-primary">Retour</a>
	{% endif %}
	{% if (app.request.attributes.get('_route') == "department_modify") %}
		<a href="{{ path('department_show',{'id':department.id}) }}" class="btn btn-sm btn-primary">Retour</a>
	{% endif %}
	
</div>