{% if app.user.title == "ROLE_SADMIN" %}
	<table class="table table-striped">
		<thead>
			<tr>
				<th scope="col">Numéro</th>
				<th scope="col">Nom</th>
				<th scope="col">Compétences</th>
				<th scope="col">Actions</th>
			</tr>
		</thead>
		<tbody>
			{% for activity in activities %}
				<tr>
					<td>{{activity.entitle}}</td>
					<td>{{activity.name}}</td>
					<td>{{activity.skill}}</td>
					<td class="py-1">{% set dataId = activity.id %}{% set dataLink = path('referentiel_activity_delete', {'id': activity.id,}) %}
						<a href="{{ path('referentiel_activity_modify', {'id': activity.id,}) }}" class="btn btn-sm btn-primary my-1" data-toggle="tooltip" data-placement="top" title="Modifier"><i class="far fa-edit"></i></a>
						{% include 'partials/modals/confirm.html.twig' %}
					</td>
				</tr>
			{% endfor %}
		</tbody>
	</table>
{% endif %}

{% if app.user.title == "ROLE_ADMIN" %}
	<table class="table table-striped">
		<thead>
			<tr>
				<th scope="col">Numéro</th>
				<th scope="col">Nom</th>
				<th scope="col">Compétences</th>
				<th scope="col">Actions</th>
			</tr>
		</thead>
		<tbody>
			{% for activity in activities %}
				<tr>
					<td>{{activity.entitle}}</td>
					<td>{{activity.name}}</td>
					<td>{{activity.skill}}</td>
					<td class="py-1">{% set dataId = activity.id %}{% set dataLink = path('referentiel_activity_delete', {'id': activity.id,}) %}
						<a href="{{ path('referentiel_activity_modify', {'id': activity.id,}) }}" class="btn btn-sm btn-primary my-1" data-toggle="tooltip" data-placement="top" title="Modifier"><i class="far fa-edit"></i></a>
						{% include 'partials/modals/confirm.html.twig' %}
					</td>
				</tr>
			{% endfor %}
		</tbody>
	</table>
{% endif %}

{% if app.user.title == "ROLE_TEACHER" %}
	<table class="table table-striped">
		<thead>
			<tr>
				<th scope="col">Numéro</th>
				<th scope="col">Nom</th>
				<th scope="col">Compétences</th>
			</tr>
		</thead>
		<tbody>
			{% for activity in activities %}
				<tr>
					<td>{{activity.entitle}}</td>
					<td>{{activity.name}}</td>
					<td>{{activity.skill}}</td>
				</tr>
			{% endfor %}
		</tbody>
	</table>
{% endif %}

{% if app.user.title == "ROLE_USER" %}
	<table class="table table-striped">
		<thead>
			<tr>
				<th scope="col">Numéro</th>
				<th scope="col">Nom</th>
				<th scope="col">Compétences</th>
			</tr>
		</thead>
		<tbody>
			{% for activity in activities %}
				<tr>
					<td>{{activity.entitle}}</td>
					<td>{{activity.name}}</td>
					<td>{{activity.skill}}</td>
				</tr>
			{% endfor %}
		</tbody>
	</table>
{% endif %}