{% extends 'base.html.twig' %}

{% form_theme form 'bootstrap_4_layout.html.twig' %}

	{% block body %}

		{# formulaire de création d'une classe #}
		{{ form_start(form) }}
		{{ form_row(form.establishment, {'label':'Sélectionner l\'établissement :'}) }}
		{{ form_row(form.degree, {'label':'Diplôme :'}) }}
		{{ form_row(form.startDate, {'label':'Date de début :'}) }}
		{{ form_row(form.endDate, {'label':'Date de fin :'}) }}
		{{ form_row(form.exist, {'label':'Activer la classe :'}) }}
		{{ form_row(form.save, {'label':'Enregistrer'}) }}
		{{ form_end(form) }}

	{% endblock %}