{% extends 'base.html.twig' %}

{% block body %}

	{# Affichage du département #}
	<div class="row">
		<div class="col-3">
			<div class="card">
				<ul class="list-group list-group-flush">
					<li class="list-group-item">Nom du département : {{ department.name | upper }}</li>
					<li class="list-group-item">Crée le : {{ department.createdAt | date("d/m/Y") }}</li>
				</ul>
			</div>
		</div>
		<div class="col-9">
			<h5>Liste des établissements :</h5>
			{% include 'partials/tables/establishment.html.twig' %}
		</div>
	</div>

{% endblock %}
